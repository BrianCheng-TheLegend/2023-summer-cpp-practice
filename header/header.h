#ifndef _LOG_H
#define _LOG_H

#pragma once //preprocessing is with #
// header guard to prevent the header include twice
#include<iostream>

void LOG(const char* message); 


struct Player{};

#endif